import 'package:flutter/material.dart';

class TambahMenuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tambah Menu")),
      body: Center(child: Text("Halaman Tambah Menu")),
    );
  }
}
